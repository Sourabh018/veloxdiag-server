spring:
  application:
    name: veloxdiag-server

  datasource:
    url: jdbc:mysql://localhost:3306/veloxdiag
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081